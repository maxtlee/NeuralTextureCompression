# Exact environment used on this machine (GTX 1050 Ti, compute capability sm_61).
#
# The default PyPI torch wheel is +cu130, which dropped Pascal and fails with
# "no kernel image is available for execution on the device". The CUDA 12.6
# build ships sm_60 kernels, which are binary-compatible with sm_61.
#
# Use this file instead of requirements.txt on this box (or any Pascal card):
#   pip install -r requirements-cu126.txt

--extra-index-url https://download.pytorch.org/whl/cu126
torch==2.14.0+cu126
torchvision==0.29.0+cu126
numpy==2.5.3
pillow==12.3.0
matplotlib==3.11.2
tqdm==4.70.1

# Portable Python dependencies for the neural texture compression project.
#
# `pip install -r requirements.txt` installs the PyTorch build for your platform
# (CUDA on most Linux/Windows GPUs, Apple MPS on macOS, CPU otherwise) plus the
# small support libraries. See the README "GPU notes" if you need a specific
# CUDA build; this machine's Pascal GTX 1050 Ti uses requirements-cu126.txt.
#
# Developed on Python 3.14; anything 3.10+ should work.

torch>=2.4
torchvision>=0.19
numpy==2.5.3
pillow==12.3.0
matplotlib==3.11.2
tqdm==4.70.1
